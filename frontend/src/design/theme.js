import { createTheme } from "@mui/material"

export const bpTheme = createTheme({
    palette: {
        primary: {
            main:"#33DBFF"
        },
        secondary: {
            main:"#FF5733"
        }
    },
    typography: {
        titles: {
            fontSize: "2rem",
            color: "#FF5733"
        }
    }

});