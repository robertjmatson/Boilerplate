import { createTheme } from "@mui/material"

export const bpTheme = createTheme({
    palette: {
        primary: {
            main:"#33DBFF"
        },
        secondary: {
            main:"#FF5733"
        }
    }
});